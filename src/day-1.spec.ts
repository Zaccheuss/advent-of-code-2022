import {assert} from "chai";
import {day1} from "./day-1";

describe("part one", () => {
  it("returns an empty string (example)", () => {
    const result = day1.solveForPartOne("");
    assert.equal(result, "");
  });
});
